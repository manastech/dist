start on starting <%= app_name %>
stop on stopping <%= app_name %>
respawn

chdir /usr/share/<%= app_name %>
env GEM_HOME=/usr/share/<%= app_name %>/.gems
env HOME=/usr/share/<%= app_name %>

script
  . /etc/default/<%= app_name %>
  .gems/bin/passenger start -e production -p $HTTP_PORT --user root
end script
